// Initialize AOS (Animate On Scroll)
AOS.init({
    once: true, 
    offset: 100,
});

// Initialize Typed.js (Efek Mengetik)
var typed = new Typed('#typed-output', {
    strings: ['Websites.', 'UI/UX Designs.'],
    typeSpeed: 50,
    backSpeed: 30,
    loop: true
});